from django.apps import AppConfig


# Class for the Authentification app
class AuthentificationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'authentification'
